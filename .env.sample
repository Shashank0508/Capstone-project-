# Amazon credentials (optional, only needed if you encounter login requirements)
AMAZON_EMAIL=your_amazon_email@example.com
AMAZON_PASSWORD=your_amazon_password

# OpenAI API key (for summary generation)
OPENAI_API_KEY=your_openai_api_key

# Flask configuration
FLASK_APP=app.py
FLASK_ENV=production
